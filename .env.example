# ===================== DOCKER IMAGE VERSIONS (Safe to commit) =====================
# Docker Hub Username
DOCKER_USERNAME=kushan7

# Service Image Tags
AUTH_SERVICE_TAG=1.0.0
PROFILE_SERVICE_TAG=1.0.0
APPOINTMENTS_SERVICE_TAG=1.0.0
PRESCRIPTION_SERVICE_TAG=1.0.0
INVENTORY_SERVICE_TAG=1.0.0
VIDEO_CONSULTATION_SERVICE_TAG=1.0.0

# ===================== NON-SECRET APPLICATION CONFIG (Safe to commit) =====================
# Environment
NODE_ENV=production

# Service Ports
AUTH_SERVICE_PORT=8080
PROFILE_SERVICE_PORT=8082
APPOINTMENTS_SERVICE_PORT=8083
PRESCRIPTION_SERVICE_PORT=8085
INVENTORY_SERVICE_PORT=8084
VIDEO_CONSULTATION_SERVICE_PORT=8086
KONG_PROXY_PORT=8000
KONG_ADMIN_PORT=8001
KONG_ADMIN_UI_PORT=8002

# Database Configuration (non-secret, local to compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
AUTH_DB_NAME=authdb
PROFILE_DB_NAME=profiledb
APPOINTMENTS_DB_NAME=appointmentsdb
PRESCRIPTION_DB_NAME=prescriptiondb
INVENTORY_DB_NAME=inventorydb
VIDEO_DB_NAME=videodb

# Database Ports
AUTH_DB_PORT=5433
PROFILE_DB_PORT=5434
APPOINTMENTS_DB_PORT=5435
PRESCRIPTION_DB_PORT=5436
INVENTORY_DB_PORT=5437
VIDEO_DB_PORT=5438

# RabbitMQ Configuration (non-secret defaults)
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# Email Configuration
EMAIL_FROM_NAME=PulseOne

# ===================== SECRETS (Add these to .env, NOT to git) =====================
# The following should be in .env file only (not committed):
# JWT_SECRET=your-secret-key
# GMAIL_SMTP_EMAIL=your-email@gmail.com
# GMAIL_SMTP_APP_PASSWORD=your-app-password
# AUTH_PUBLIC_BASE_URL=http://localhost:8000
# STREAM_API_KEY=your-stream-api-key
# STREAM_API_SECRET=your-stream-api-secret
