apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: profile-postgres-data
  name: profile-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi