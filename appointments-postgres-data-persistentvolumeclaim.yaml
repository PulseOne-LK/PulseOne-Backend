apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: appointments-postgres-data
  name: appointments-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi